apiVersion: v1
kind: Secret
metadata:
  name: easybank-secrets
type: Opaque
data:
  # These values should be provided through environment variables or a secure secret management system
  db-username: ${DB_USERNAME}
  db-password: ${DB_PASSWORD} 